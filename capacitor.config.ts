import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mercadoia.flex',
  appName: 'Mercado IA Flex',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;